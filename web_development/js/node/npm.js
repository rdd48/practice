// npm (node package manager) for installing packages
// run npm install my-package-name
// also npm i my-package-name
// global install using -g flag: npm i -g my-package-name
// then npm link my-package-name
// then in script: const myPkg = require('myPkg')
// console.dir(myPkg) tells you what you imported

// to make a package.json, in command line:
// npm init (then answer through prompts. main: 'index.js')

// install all requirements from a package.json, cd into a dir with that file then in cl:
// npm install